describe('dummy tests', () => {
  it('should be true', () => {
    expect(true).toBeTruthy();
  });

  it('should support typescript', () => {
    const typescript: string = 'typescript';
    expect(typescript).toEqual('typescript');
  });
})