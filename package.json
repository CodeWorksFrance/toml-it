{
  "name": "toml-integration-tests",
  "version": "1.0.0",
  "main": "./lib/index.js",
  "license": "MIT",
  "dependencies": {
    "glob": "^8.0.3",
    "toml": "^3.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.8.4"
  },
  "files": [
    "./bin/*",
    "./lib/*"
  ],
  "typings": "./lib/index.d.ts",
  "scripts": {
    "prebuild": "jest --coverage",
    "build": "tsc",
    "start": "node ./lib/",
    "test": "jest",
    "coverage": "jest --coverage",
    "lint": "tslint -c tslint.json src/**/*.ts",
    "prepublish": "npm run build"
  },
  "devDependencies": {
    "@types/glob": "^8.0.0",
    "@types/jest": "^29.2.2",
    "jest": "^29.3.0",
    "ts-jest": "^29.0.3"
  }
}
